import 'package:flutter/material.dart';

class AppColumna extends StatelessWidget {
  const AppColumna({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}